@using Microsoft.AspNet.Identity
@{

    WebApplication3.Models.ApplicationDbContext db = new WebApplication3.Models.ApplicationDbContext();
    WebApplication3.Models.ApplicationUser applicationUser = db.Users.Find(User.Identity.GetUserId());
    WebApplication3.Models.Student student = db.Students.Find(User.Identity.GetUserId());
    if (Request.IsAuthenticated && User.IsInRole("ADMIN"))
    {
        Layout = "~/Views/Shared/_Layout.cshtml";

    }
    else if (Request.IsAuthenticated && User.IsInRole("TEACHER"))
    {
        Layout = "~/Views/Shared/_LayoutTeacher.cshtml";

    }

    else if (Request.IsAuthenticated)
    {
        Layout = "~/Views/Shared/_LayoutStudent.cshtml";

    }
    else {
        Layout = "~/Views/Shared/_Layout.cshtml";
    }


}
